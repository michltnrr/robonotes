<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Robonotes</title>
        <link rel="icon" href="robonotesnewlogo2.png" type="image/png">
        <link rel="stylesheet" href="/css/header.css">
        <link rel="stylesheet" href="/css/footer.css">
    </head>

    <body>
        {{>header}}

        <!-- Floating red/pink orb -->
        <div class="floating-orb"></div>

        <div class="main-content">
            <h1>Welcome to your study hub</h1>
        </div>

        {{>footer}}
    </body>

    <style>
        body {
            background-color: #101010;
            color: white;
            font-family: 'Roboto', sans-serif;
            margin: 0;
            overflow: hidden; /* prevents scrollbars from orb movement */
        }

        /* Floating circular orb */
        .floating-orb {
            position: fixed;
            top: 50%;
            left: 50%;
            width: 400px;
            height: 400px;
            border-radius: 50%;
            background: radial-gradient(
                circle,
                rgba(255, 100, 100, 0.6),
                rgba(255, 150, 150, 0.45),
                transparent 65%
            );
            filter: blur(50px);
            pointer-events: none;
            z-index: 0;

            /* smooth motion + subtle color shift */
            animation: moveOrb 12s linear infinite, subtleColor 8s ease-in-out infinite alternate;
        }

        /* Smooth circular-ish motion */
        @keyframes moveOrb {
            0% { transform: translate(-50%, -50%) translateX(0px) translateY(0px); }
            25% { transform: translate(-50%, -50%) translateX(120px) translateY(40px); }
            50% { transform: translate(-50%, -50%) translateX(0px) translateY(80px); }
            75% { transform: translate(-50%, -50%) translateX(-120px) translateY(40px); }
            100% { transform: translate(-50%, -50%) translateX(0px) translateY(0px); }
        }

        /* Subtle red → pink color animation */
        @keyframes subtleColor {
            0% {
                background: radial-gradient(circle, rgba(255,100,100,0.6), rgba(255,150,150,0.45), transparent 65%);
            }
            50% {
                background: radial-gradient(circle, rgba(255,110,110,0.6), rgba(255,165,165,0.45), transparent 65%);
            }
            100% {
                background: radial-gradient(circle, rgba(255,100,100,0.6), rgba(255,150,150,0.45), transparent 65%);
            }
        }

        .main-content {
            position: relative;
            z-index: 1; /* content above orb */
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* Blue → Pink gradient for heading */
        h1 {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(to right, rgb(45,168,210), rgb(217,59,85));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-top: 40px;
        }
    </style>
</html>
