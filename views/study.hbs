<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Robonotes - Study Hub</title>
    <link rel="icon" href="robonotesnewlogo2.png" type="image/png">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">
    <style>
        body {
            background-color: #000000;
            color: white;
            font-family: 'Roboto', sans-serif;
        }

        /* Main content centered */
        .main-content {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            margin-top: 40px;
        }

        #greeting {
            font-size: 2.5rem;
            font-weight: 700;
            background: linear-gradient(to right, #2da8d2, #d93b55);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 8px;
        }

        .subtitle {
            color: #888;
            font-size: 1rem;
            font-weight: 400;
            margin-bottom: 24px;
        }

        .input-container {
            width: 100%;
            max-width: 800px;
            display: flex;
            justify-content: center;
            margin-bottom: 24px;
        }

        .input-wrapper {
            background: #1a1a1a;
            border: 1px solid #2a2a2a;
            border-radius: 28px;
            padding: 6px;
            width: 100%;
            box-shadow: 0 4px 24px rgba(0,0,0,0.4);
        }

        .input-wrapper:focus-within {
            border-color: #2da8d2;
            box-shadow: 0 4px 32px rgba(45,168,210,0.15);
        }

        .main-input {
            width: 100%;
            background: transparent;
            border: none;
            outline: none;
            color: #e8e8e8;
            font-size: 1.05rem;
            padding: 16px 24px;
            font-family: inherit;
            resize: none;
            min-height: 56px;
            max-height: 200px;
        }

        .main-input::placeholder {
            color: #666;
        }

        .input-footer {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 16px 8px;
        }

        .mode-selector {
            display: flex;
            gap: 8px;
        }

        .mode-btn {
            background: #252525;
            border: 1px solid #333;
            color: #888;
            padding: 6px 14px;
            border-radius: 16px;
            font-size: 0.875rem;
            cursor: pointer;
            transition: all 0.2s ease;
            font-weight: 500;
        }

        .mode-btn:hover { background: #2d2d2d; color: #aaa; }
        .mode-btn.active { background: #2da8d2; border-color: #2da8d2; color: #fff; }

        .submit-btn {
            background: #2da8d2;
            border: none;
            color: #fff;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.2s ease;
            font-size: 1.2rem;
        }

        .submit-btn:hover { background: #2190b0; transform: scale(1.05); }
        .submit-btn:active { transform: scale(0.95); }

        .output-section {
            width: 100%;
            max-width: 800px;
            margin-top: 40px;
            display: none;
            flex-direction: column;
            align-items: center;
        }

        .output-section.active { display: flex; }

        .output-content {
            background: #141414;
            border: 1px solid #252525;
            border-radius: 16px;
            padding: 28px;
            min-height: 200px;
            color: #e8e8e8;
            line-height: 1.7;
            font-size: 1rem;
            white-space: pre-wrap;
            width: 100%;
            max-width: 600px;
        }

        .output-content:focus { outline: none; border-color: #d55193; }

        .output-content:empty:before {
            content: 'Generated content will appear here...';
            color: #555;
        }

        /* Quiz container */
        .quiz-container {
            width: 100%;
            max-width: 600px;
            margin: 16px auto 0 auto;
            background-color: #1a1a1a;
            border: 1px solid #252525;
            border-radius: 12px;
            padding: 16px;
        }
        .quiz-container from {
            margin-bottom: 20px;
        }

        .quiz-question {
            margin: 0 0 12px 0;
            font-weight: 500;
            font-size: 0.95rem;
            text-align: left;
            color: #e8e8e8;
        }

        .quiz-option {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 6px 12px;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.2s ease;
            font-size: 0.9rem;
            background-color: rgba(255, 255, 255, 0.03);
        }

        .quiz-option:hover { background-color: rgba(255, 255, 255, 0.08); }

        .quiz-option input[type="radio"] { cursor: pointer; }

        .quiz-option label { cursor: pointer; font-family: 'Roboto', sans-serif; }

        @media (max-width: 768px) {
            .mode-selector { flex-wrap: wrap; }
            .output-content, .quiz-container { max-width: 100%; }
        }
    </style>
</head>
<body>
    {{>header}}

    <div class="main-content">
        <h1 id="greeting">Welcome to your study hub</h1>
        <p class="subtitle">Generate descriptive visualizations, quizzes, and summaries instantly</p>

        <div class="input-container">
            <div class="input-wrapper">
                <textarea 
                    class="main-input" 
                    id="mainInput"
                    placeholder="Visualize the mechanics of hydrostatic force..."
                    rows="1"
                ></textarea>
                <div class="input-footer">
                    <div class="mode-selector">
                        <button class="mode-btn active" data-mode="video">📹 Video Generator</button>
                        <button class="mode-btn" data-mode="quiz">📚 Quiz Generator</button>
                        <button class="mode-btn" data-mode="summary">✍🏼 Summary Generator</button>
                    </div>
                    <button class="submit-btn" id="submitBtn">↑</button>
                </div>
            <!-- Quiz container -->
            <div class="quiz-container" id="quizContainer" hidden>
                <form class="quiz-contents">
                    <p class="quiz-question">1. Which of the following is used to declare a variable in JavaScript?</p>

                    <div class="quiz-option">
                        <input type="radio" name="question1" id="q1-a">
                        <label for="q1-a">A. var</label>
                    </div>

                    <div class="quiz-option">
                        <input type="radio" name="question1" id="q1-b">
                        <label for="q1-b">B. let</label>
                    </div>

                    <div class="quiz-option">
                        <input type="radio" name="question1" id="q1-c">
                        <label for="q1-c">C. const</label>
                    </div>

                    <div class="quiz-option">
                        <input type="radio" name="question1" id="q1-d">
                        <label for="q1-d">D. all of the above</label>
                    </div>
                </form>
            </div>
            </div>
        </div>

        <div class="output-section" id="outputSection">
            <div class="output-content" id="outputContent" contenteditable="true"></div>

        </div>
    </div>

    {{>footer}}

    <script src="../study.js"></script>
</body>
</html>